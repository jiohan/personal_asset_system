## Summary
- 

## Checklist
- [ ] `git status` is clean (only intended changes)
- [ ] Infra/dev env changes have `.env.example` (no real secrets committed)
- [ ] If schema/report logic changed: tests/SQL checks included (or reason noted)
- [ ] Docs updated if behavior changed (`README.md`, `docs/*`)
